export enum Gender {
  Male = 'Male',
  Female = 'Female',
  NotSpecified = 'Not Specified',
}

export enum SignUpType {
  admin = 'admin',
  shopper = 'shopper',
  brand = 'brand',
}

export enum Role {
  Shopper = 'shopper',
  Brand = 'brand',
}
