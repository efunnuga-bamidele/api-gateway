export enum Gender {
  Male = 'Male',
  Female = 'Female',
  NotSpecified = 'Not Specified',
}

export enum SignUpType {
  admin = 'admin',
  shopper = 'shopper',
  vendor = 'vendor',
}

export enum Role {
  Shopper = 'shopper',
  Vendor = 'vendor',
}
