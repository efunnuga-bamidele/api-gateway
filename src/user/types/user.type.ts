export enum Gender {
  Male = 'Male',
  Female = 'Female',
  NotSpecified = 'Not Specified',
}

export enum SignUpType {
  admin = 'admin',
  customer = 'customer',
  vendor = 'vendor',
}

export enum Role {
  Customer = 'customer',
  Vendor = 'vendor',
}
